# Copie este arquivo para backend/.env e preencha os valores.
# O .env real NUNCA e commitado (ver .gitignore e CLAUDE.md).

SECRET_KEY=troque-por-um-valor-aleatorio
DEBUG=True

# Pending Decision: o formato de DATABASE_URL depende do ORM aprovado pela equipe.
# Alvo declarado no CLAUDE.md: PostgreSQL via Docker Compose.
DATABASE_URL=
